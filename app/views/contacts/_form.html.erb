
<%= simple_form_for @contact do |f| %>
    <%= f.input :name, label: 'Contact' %>
    <%= f.input :email, label: 'Email' %>
    <%= f.input :message, label: 'Message' %>
    <%= f.button :submit, class: "btn btn-primary btn-lg active" %>
<% end %>
